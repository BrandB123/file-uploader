<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>File Details</title>
</head>
<body>
    <%- include("navbar", { user: user }); %>
    <p><%= file.name%></p>
    <p><%= file.date_added%></p>
    <% if (file.share_id) { %> 
        <p><a href="/folders-files/shared/<%=user.id%>/<%=file.share_id%>" target="_blank">Shareable Link</a></p>
    <%}%>

    <form action="/folders-files/files/download" method="post">
        <input type="text" name="downloadFile" value="<%= file.path %>" 
               style="visibility: hidden;" />
        <button type="submit">Download</button>
    </form>

    <form action="/folders-files/files/share" method="post">
        <input type="text" name="shareFile" value="<%= file.path %>" 
               style="visibility: hidden;" />
        <input type="date" name="shareExp" required/>
        <button type="submit">Share</button>
    </form>

    <form action="/folders-files/files/delete" method="post">
        <input type="text" name="deleteFile" value="<%= file.path %>" 
               style="visibility: hidden;" />
        <button type="submit">Delete</button>
    </form>
</body>
</html>